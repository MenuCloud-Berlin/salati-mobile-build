// Eigenstaendige Route fuer die Gebetszeiten — auf Web die einzige
// Moeglichkeit, den Screen zu sehen (das Tab-Home '/' zeigt dort die
// Marketing-Landingpage). Auf nativ ungenutzt, aber harmlos.
export { default } from '@/components/prayer-times-screen';
